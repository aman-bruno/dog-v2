info:
  name: echo
  type: http
  seq: 4

http:
  method: POST
  url: http://echo.usebruno
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
